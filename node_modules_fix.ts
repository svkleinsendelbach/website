const fs = require('fs');

const replacements: { file: string; replace: string }[] = [
    {
        file: 'node_modules\\@kolkov\\angular-editor\\lib\\angular-editor.module.d.ts',
        replace: '@kolkov_angular_editor_angular_editor_module.d.ts'
    },
    {
        file: 'node_modules\\@widgetbot\\crate\\dist\\api\\types.d.ts',
        replace: '@widgetbot_crate_api_types.d.ts'
    }
];

for (const replacement of replacements) {
    const content = fs.readFileSync(`node_modules_fix\\${replacement.replace}`, 'utf8');
    fs.writeFileSync(replacement.file, content, 'utf8');
}